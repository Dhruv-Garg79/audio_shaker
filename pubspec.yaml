name: audio_shaker
description: A music player which can be controller using gestures.
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.0.3 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  stacked: ^3.4.0
  stacked_services: ^1.1.0
  sensors_plus: ^4.0.2
  just_audio: ^0.9.36
  dio: ^5.4.0
  logger: ^1.4.0
  cached_network_image: ^3.3.1

dev_dependencies:
  build_runner: ^2.4.5
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.1
  mockito: ^5.4.1
  stacked_generator: ^1.3.3

flutter:
  uses-material-design: true

